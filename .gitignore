# Pipeline data outputs (generated at runtime — not committed)
data/
output/
logs/

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# Environment / secrets
.env
.env.*
*.pem
*.key

# Jupyter
.ipynb_checkpoints/

# Spark
derby.log
metastore_db/
spark-warehouse/

# Google Cloud credentials
application_default_credentials.json
*-credentials.json

# Azure ML workspace config (contains subscription ID — never commit)
azure-ml/config.json
azure-ml-outputs/

# dbt (profiles.yml is machine-specific; target/ and dbt_packages/ are generated)
dbt_aml/profiles.yml
dbt_aml/target/
dbt_aml/dbt_packages/
dbt_aml/logs/

# macOS / Windows
.DS_Store
Thumbs.db
