# ── Apache Airflow core ───────────────────────────────────────────────────────
apache-airflow==2.9.3

# ── Airflow extras bundled with the above pin ─────────────────────────────────
# celery, redis, postgres are not used here (LocalExecutor + Postgres metadata)
# but psycopg2 is required for the PostgreSQL backend connection.
apache-airflow[postgres]==2.9.3

# ── Database driver ───────────────────────────────────────────────────────────
psycopg2-binary==2.9.9

# ── PySpark (must match the version in docker-compose.airflow.yml) ────────────
pyspark==3.5.1
